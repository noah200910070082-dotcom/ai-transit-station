# AI 中转站产品设计基准

## 产品定位

AI 中转站是一个面向普通 API 使用者、独立开发者与小团队的 AI API 管理控制台。产品使用 `new-api` 作为真实后端，核心任务是让用户快速完成注册、获取令牌、查看可用模型、检查消费日志、充值与管理账户。

## 体验目标

- 新用户登录后能立即判断余额、调用状态和下一步操作。
- 高频任务保持在左侧导航中，页面内容以扫描、筛选和操作效率为优先。
- 普通用户只看到自己的数据；管理员账号登录后自然获得用户、渠道和系统数据入口。
- 所有后端状态均来自真实接口。无数据时显示明确空状态，不使用演示数字冒充真实数据。
- 繁体中文、简体中文和英文保持同等可用。

## 视觉方向

采用 A6API 的专业控制台密度、Right Code 的清晰信息层级，以及 new-api 开源界面的功能词汇。

- 冷白与浅灰为主表面，青绿色为主强调色，蓝色仅用于信息状态。
- 侧栏紧凑、分组清晰；顶部栏只保留页面标题、语言、主题和账户操作。
- 卡片只用于指标和独立信息对象，不把每个页面区块都做成浮动卡片。
- 表格、筛选器、标签与空状态优先可读性和操作效率。
- 圆角不超过 8px，阴影克制，禁止彩虹渐变、玻璃拟态和装饰性光球。

## 信息架构

- 总览：余额、已用额度、请求数、令牌数、快速开始、最近调用。
- 使用日志：统计、筛选、真实日志表格。
- API 令牌：令牌列表、状态、额度和最近使用时间。
- 模型广场：当前账户可调用的真实模型列表。
- 充值中心：余额、兑换码、后端开放的支付配置。
- 邀请好友：邀请码与邀请数据，不放入侧栏，位于账户相关页面内容中。
- 账户设置：账户资料、分组和后端连接信息。
- 管理员：用户、渠道与系统数据，仅管理员可见。

## 技术边界

- 前端 React + TypeScript + Vite，图标使用 `lucide-react`。
- 前端同源调用 `/api`；本地 Vite proxy 到 new-api，Vercel 代理到 VPS。
- 不复制参考站品牌、文案或独有资产，只学习信息层级与交互模式。
- 优先使用 new-api 已有接口与数据模型，不另外实现 gateway、计费、限流或账号池。
